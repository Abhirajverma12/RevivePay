services:
  # Backend NestJS API Web Service
  - type: web
    name: revivepay-api
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm install && npm run build:backend
    startCommand: npx prisma db push && npm run demo:seed && npm run start:backend
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: revivepay-db
          property: connectionString

databases:
  # Managed Free PostgreSQL Database
  - name: revivepay-db
    plan: free
    region: oregon
    databaseName: revivepay
    user: revivepay_user
